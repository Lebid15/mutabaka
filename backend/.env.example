# Backend environment
# Use DJANGO_DEBUG for clarity; set to False in production
DJANGO_DEBUG=True
SECRET_KEY=changeme
# Hosts allowed to serve the app (comma-separated)
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
# CSRF trusted origins (comma-separated). Include scheme (http/https).
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:3000

# Cookies/security (optional overrides)
# CSRF_COOKIE_DOMAIN=.mutabaka.com

# Pusher Channels (Sandbox ok)
PUSHER_APP_ID=xxxxxxxx
PUSHER_KEY=xxxxxxxx
PUSHER_SECRET=xxxxxxxx
PUSHER_CLUSTER=eu
