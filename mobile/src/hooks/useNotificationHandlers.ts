/**
 * هذا الخطاف كان يُستخدم سابقاً لإدارة التنقل عبر الإشعارات.
 * تم نقل التنفيذ إلى `App.tsx` مع خدمة التنقل العامة.
 * نُبقيه كدالة فارغة للحفاظ على التوافق إن تم استيراده مستقبلاً.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-function
export function useNotificationHandlers() {}
