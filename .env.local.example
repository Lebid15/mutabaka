# Example environment variables for frontend (Next.js)
# Copy to .env.local and adjust as needed.
NEXT_PUBLIC_API_HOST=127.0.0.1
NEXT_PUBLIC_API_PORT=8000
NEXT_PUBLIC_API_PROTO=http
# Override for secure websockets if you use reverse proxy with TLS
# NEXT_PUBLIC_WS_PROTO=wss
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=eu
